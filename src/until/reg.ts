export default {
    // 手机号码
    phoneCn: /^134[0-8]\d{7}$|^13[^4]\d{8}$|^14[5-9]\d{8}$|^15[^4]\d{8}$|^16[6]\d{8}$|^17[0-8]\d{8}$|^18[\d]{9}$|^19[8,9]\d{8}$/,
    phone: /^(\+?81|0)\d{1,4}[ \-]?\d{1,4}[ \-]?\d{4}$/,
    //0.01~1之间的小数，eg:折扣率
    zeroOne: /(^0\.[1-9][0-9]{0,1}$)|(^0\.0[1-9]{1}$)|(^1((\.0)|(\.00))?$)/,
    //数字
    number: /^\d+$/,
    //价格 不能为0
    price:/(^[1-9]([0-9]+)?(\.[0-9]{1,2})?$)|(^[0-9]\.[0-9]([0-9])?$)/,
    //价格 可以为0
    zeroPrice:/(^[1-9]([0-9]+)?(\.[0-9]{1,2})?$)|(^(0){1}$)|(^[0-9]\.[0-9]([0-9])?$)/,
    //数字,不包含0
    noZeroNumber: /^[1-9]\d{0,}$/,
    // 固定电话
    telephone: /(^\d{0,9}-\d{0,10}$)|(^\d{1,20}$)/,
    // 纳税人识别号
    tax: /^[A-Za-z0-9]{15,20}$/,
    // 银行户号
    bankNumber: /^([1-9]{1})(\d{15}|\d{18})$/,
    //仅中文或英文，不允许含有数字
    noNumber: /^\D*$/,
    //数字或者英文，没有符号
    numberOrLetter : /^[0-9a-zA-Z]*$/,
    //电话，座机，数字加-
    numberOr_:/^[0-9-]*$/,
    //电子邮箱
    mailbox:/^([a-zA-Z0-9]+[_|\_|\.]?)*[a-zA-Z0-9]+@([a-zA-Z0-9]+[_|\_|\.]?)*[a-zA-Z0-9]+\.[a-zA-Z]{2,3}$/,
    //邮编
    // postalCode:/^[1-9][0-9]{5}$/
    postalCode:/^([0-9]{3}-)?[0-9]{4}$/
  }